{
  "name": "@bulky-example/aws-php",
  "version": "0.0.0",
  "dependencies": {
    "@bulky/aws-s3": "workspace:*",
    "@bulky/core": "workspace:*",
    "@bulky/dashboard": "workspace:*",
    "bulky": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "^0.15.1"
  },
  "private": true,
  "type": "module",
  "scripts": {
    "start": "php -S localhost:8080 serve.php",
    "outputBundle": "esbuild --format=esm --sourcemap=inline --bundle ./main.js"
  }
}
